name: 01-building-blocks
on: workflow_dispatch

jobs:
   echo-hello:
      runs-on: ubuntu-latest
      steps:
        - run: echo "Hello world"

   echo-goodbye:
      runs-on: ubuntu-latest
      steps:
        - name: Failed step 
          run: |
            echo "i will suceed"
            exit 0
        - name: say goodbye
          run: echo "goodbye"        